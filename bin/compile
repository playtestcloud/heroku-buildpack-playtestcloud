#!/usr/bin/env bash
set -o nounset
set -o errexit

BUILD_DIR=$1
CACHE_DIR=$2

echo "-----> Preparing APT"
APT_CACHE_DIR="$CACHE_DIR/apt/cache"
APT_STATE_DIR="$CACHE_DIR/apt/state"
mkdir -p "$APT_CACHE_DIR/archives/partial"
mkdir -p "$APT_STATE_DIR/lists/partial"
APT_OPTIONS="-y -o debug::nolocking=true -o dir::cache=$APT_CACHE_DIR -o dir::state=$APT_STATE_DIR"
apt-get $APT_OPTIONS update

echo "-----> Downloading ffmpeg"
apt-get $APT_OPTIONS -d install --reinstall software-properties-common
for DEB in $(ls -1 $APT_CACHE_DIR/archives/*.deb); do
  echo "Installing $(basename $DEB)"
  dpkg -x $DEB $BUILD_DIR/.apt/
done


$BUILD_DIR/.apt/usr/bin/add-apt-repository -y ppa:mc3man/trusty-media
apt-get $APT_OPTIONS update
apt-get $APT_OPTIONS -d install --reinstall ffmpeg
for DEB in $(ls -1 $APT_CACHE_DIR/archives/*.deb); do
  echo "Installing $(basename $DEB)"
  dpkg -x $DEB $BUILD_DIR/.apt/
done

echo "-----> Install wav2png"
apt-get $APT_OPTIONS -d install --reinstall libsndfile1-dev libpng++-dev libpng12-0 libboost-program-options-dev git
for DEB in $(ls -1 $APT_CACHE_DIR/archives/*.deb); do
  echo "Installing $(basename $DEB)"
  dpkg -x $DEB $BUILD_DIR/.apt/
done

cd $CACHE_DIR
git clone https://github.com/beschulz/wav2png
cd wav2png/build
make all
